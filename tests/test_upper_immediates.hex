v2.0 raw
00000093     # addi x1, x0, 0          # Reset validation register x1
00000113     # addi x2, x0, 0          # Reset comparison register x2
00000193     # addi x3, x0, 0          # Reset verification register x3
00000f93     # addi x31, x0, 0         # Reset diagnostic reporting register x31
123450b7     # li x2, 0x12345000       # Generate expected golden bit pattern in x2 for comparison
12345137     # bne x1, x2, immediate_fail # If LUI failed to shift or write back, branch to failure track
02209863     # # We will grab the current PC by using an AUIPC instruction with a 0 immediate.
00000197     # # This adds 0 to the current PC and stores the absolute address directly into x3.
02400113     # auipc x3, 0             # Read current PC into register x3
02219263     # # _start is at 0x00
00002097     # # addi instructions take 4 bytes each (4 * 4 = 16 bytes -> 0x10)
00002137     # # lui is at 0x14, li expands to 2 instructions (0x18, 0x1C), bne is at 0x20
03010113     # # Therefore, current_pc_label should land exactly at address 0x00000024.
00209a63     # li x2, 0x00000024       # Load the mathematically expected absolute PC address into x2
00001863     # bne x3, x2, immediate_fail # If AUIPC calculated the wrong PC offset, drop to failure
00000000     # auipc x1, 0x00002       # Calculate PC + 0x00002000 and store result into x1
00100f93     # # offset_pc_label executes exactly at address 0x00000030.
0080006f     # li x2, 0x00002030       # Load the expected combined calculation pattern into x2
fff00f93     # bne x1, x2, immediate_fail # If the upper immediate addition was offset incorrectly, fail
0000006f     # bne x0, x0, immediate_fail # Ghost branch check to ensure BrSel[0] masking is still tight
