v2.0 raw
00000093     # addi x1, x0, 0          # Reset base memory pointer register x1
00000113     # addi x2, x0, 0          # Reset source register x2
00000193     # addi x3, x0, 0          # Reset verification loading register x3
00000213     # addi x4, x0, 0          # Reset reference comparison register x4
00000f93     # addi x31, x0, 0         # Reset diagnostic reporting register x31
08000093     # addi x1, x0, 128        # Set base memory pointer to address 128 (0x80 - safely within 256 bytes)
12345137     # li x2, 0x12345678       # Load 32-bit test pattern into x2
67810113     # sw x2, 0(x1)            # Store full word to initialize RAM at that location
0020a023     # # =========================================================================
09600113     # # 1. TEST BYTE STORE AND SIGNED BYTE LOAD (sb, lb)
00208023     # # =========================================================================
00008183     # addi x2, x0, 0x96       # Load an 8-bit pattern with the MSB set (negative if signed)
f9600213     # sb x2, 0(x1)            # Overwrite byte 0 at RAM[128] with 0x96
04419663     # lb x3, 0(x1)            # Load signed byte from RAM[128]
0000c183     # li x4, 0xFFFFFF96       # Expected value must be sign-extended to 32 bits
09600213     # bne x3, x4, mem_fail    # Fail if sign-extension failed on lb
04419063     # # =========================================================================
0000b137     # # 2. TEST UNSIGNED BYTE LOAD (lbu)
bcd10113     # # =========================================================================
00209123     # lbu x3, 0(x1)           # Load unsigned byte from RAM[128]
00209183     # addi x4, x0, 0x96       # Expected value must be zero-extended to 0x00000096
ffffb237     # bne x3, x4, mem_fail    # Fail if zero-extension failed on lbu
bcd20213     # # =========================================================================
02419263     # # 3. TEST HALF-WORD STORE AND SIGNED HALF-WORD LOAD (sh, lh)
0020d183     # # =========================================================================
0000b237     # li x2, 0xABCD           # Load a 16-bit pattern with the MSB set
bcd20213     # sh x2, 2(x1)            # Overwrite upper half-word at RAM[130] with 0xABCD
00419a63     # lh x3, 2(x1)            # Load signed half-word from RAM[130]
00001863     # li x4, 0xFFFFABCD       # Expected value must be sign-extended to 32 bits
00000000     # bne x3, x4, mem_fail    # Fail if sign-extension failed on lh
00100f93     # # =========================================================================
0080006f     # # 4. TEST UNSIGNED HALF-WORD LOAD (lhu)
fff00f93     # # =========================================================================
0000006f     # lhu x3, 2(x1)           # Load unsigned half-word from RAM[130]
